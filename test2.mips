.text
.globl main
main:
	# cargamos los vectores
	la $a0, a
	la $a1, b

	# cargamos el valor
	lw $a2, 0($a0)
	lw $a3, 0($a1)
	
	# comparamos
	slt $a4, $a2, $a3 # a4 if a2<a3
	
	# salto condicional >=
	bne $a4, $0, menor # if 1!=2 salto 

	#hacer algo si es menor
#	j fin

menor:
	# mostramos un 1
	addi $a0, $0, 1	
	addi $v0, $0, 1
	syscall
#fin:
#	# mostramos un 0
#	addi $a0, $0, 1	
#	addi $v0, $0, 1
#	syscall

.data
	a: .int 3
	b: .int 4
